{
  "name": "lisp",
  "version": "0.0.0",
  "description": "",
  "main": "lisp.js",
  "scripts": {
    "test": "$(npm bin)/mocha --require coffeescript/register spec/specs.coffee && $(npm bin)/coffee src/lisp.coffee -f spec/specs.lisp"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^2.3.0",
    "mocha": "^2.5.3"
  },
  "dependencies": {
    "coffeescript": "^2.4.1",
    "colors": "^1.0.3",
    "minimist": "^1.1.1"
  }
}
